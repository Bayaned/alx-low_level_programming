#include <stdlib.h>
#include <time.h>
/**
 * mai - Prints all single digit numbers of base 10 starting from 0
 * Return : 0 (Success)
 */
int main(void)
{
        int n=0
        while(i<10)
        {
          printf("%d", i);
          i++;
	}
        return (0);
}
